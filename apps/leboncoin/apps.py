from django.apps import AppConfig


class LeboncoinConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.leboncoin'
    verbose_name = 'LeBonCoin — annuaire local'
